package com.ahmet;

@SuppressWarnings("unused")
public class LocalVariableSyntaxForLambda {
//    List<String> list = Arrays.asList("Ali", "Veli", "\n", " ", "Asda", null);
//    List<String> toUpperCase = list.stream()
//            .map((@NotNull var s) -> s.toUpperCase()).toList(); // etc.
}
